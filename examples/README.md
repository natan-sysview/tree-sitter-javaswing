# Examples

This directory contains synthetic public examples for smoke testing and demonstrations.

The examples are intentionally small and generic. They are not copied from private projects or customer source code.

Parse the showcase example:

```sh
npm run example:parse
```
